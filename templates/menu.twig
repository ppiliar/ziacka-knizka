<nav class="navbar navbar-expand-lg navbar-dark bg-info">
    <div class="container">
        <a class="navbar-brand" href="#">Žiacka knižka</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item {{ request.controller == 'index' ? 'active' }}">
                    <a class="nav-link" href="{{ baseUrl }}/">Úvod</a>
                </li>
                {% if loggedUserRole == 'admin' %}
                    <li class="nav-item {{ request.controller == 'userMan' ? 'active' }}">
                        <a class="nav-link" href="{{ baseUrl }}/userMan">Používatelia</a>
                    </li>
                    <li class="nav-item {{ request.controller == 'classMan' ? 'active' }}">
                        <a class="nav-link" href="{{ baseUrl }}/classMan">Triedy</a>
                    </li>
                    <li class="nav-item {{ request.controller == 'subjectMan' ? 'active' }}">
                        <a class="nav-link" href="{{ baseUrl }}/subjectMan">Predmety</a>
                    </li>
                {% endif %}
                {% if loggedUserRole == 'teacher' %}
                    <li class="nav-item {{ request.controller == 'teacherMan' ? 'active' }}">
                        <a class="nav-link" href="{{ baseUrl }}/teacherMan">Moje predmety</a>
                    </li>
                {% endif %}
                {% if loggedUserRole == 'student' %}
                    <li class="nav-item {{ request.controller == 'studentMan' ? 'active' }}">
                        <a class="nav-link" href="{{ baseUrl }}/studentMan">Moje znamky</a>
                    </li>
                {% endif %}
            </ul>
            {% if loggedUser %}
            <ul class="nav navbar-nav mr-5">
                <li class="nav-item">
                    <a class="nav-link" href="{{ baseUrl }}/user/settings">Nastavenia</a>
                </li>
            </ul>
            {% endif %}
            <ul class="navbar-nav">
                <li class="nav-item active">
                    {% if not loggedUser %}
                        <a class="nav-link" href="{{ baseUrl }}/user/login"><i class="fa fa-sign-in"></i> Prihlásiť sa</a>
                    {% else %}
                        <div class="badge badge-info">Prihlásený používateľ {{ loggedUser }} [{{ loggedUserRole }}]</div>
                        <a class="nav-link" href="{{ baseUrl }}/user/logout"><i class="fa fa-sign-out"></i> Odhlásiť sa</a>
                    {% endif %}
                </li>
            </ul>
        </div>
    </div>
</nav>